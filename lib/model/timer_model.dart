import 'package:flutter/material.dart';

class Task {
  final int id;
  final Color color;
  final int minutes, seconds;

  Task({
    this.id = 0,
    this.color,
    this.minutes,
    this.seconds
  });
}